#include <stdio.h>

/**
 * main - this is the main function
 *
 * Return: 0 when successfull
 */
int main(void)
{
	int i;
	int n;

	for (n == 0; n <= 10; n++)
	{
		putchar(n);
	}

	for (i == 'a'; i < 'g'; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
