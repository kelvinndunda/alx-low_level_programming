#include <stdio.h>
/**
 * main - this is the main function
 *
 * Return: 0 when successfull
 */
int main(void)
{
	int c;

	for (c == 'a'; c <= 'z'; c++)
	{
		putchar(c);
		for (c == 'A'; c <= 'Z'; c++)
			putchar(c);
	}
	putchar('\n');
	return (0);
}
