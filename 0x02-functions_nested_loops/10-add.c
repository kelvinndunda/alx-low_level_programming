#include "main.h"

/**
 * add - function to add values
 * @x: one of the int parameters
 * @y: the onther int parameter
 *
 * Return: total of the integers entered
 */

int add(int x, int y)
{
	int total = x + y;

	return (total);
}
