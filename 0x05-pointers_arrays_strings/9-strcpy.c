#include"main.h"
#include<string.h>

/**
 **_strcpy - this is copying a string
 *@src: the string to be copyed
 *@dest: the pointer to be pasted LOL
 *
 *Return: dest.
 **/

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);

	return (dest);
}
